# Copy this file to .env and fill in real values for local dev.
# On Vercel, set this same key under Project Settings > Environment Variables instead — it must point at your
# deployed Render backend URL (e.g. https://sensora-api.onrender.com/api), not a LAN IP or localhost.
VITE_API_URL=http://localhost:5000/api
